---

- name: install git
  hosts: workers
  tasks:
    command: "sudo dnf install git -y"
    become: yes

- name: clone case2 repo
  hosts: workers
  git:
    repo: "https://@github.com/{{ user }}/{{ repo_name }}.git"
    dest: "{{ target_dir }}"
  no_log: true